

const initialState = {
    login: null,
    auth: ''
};

const authenticationReducer = (state = initialState, actions) => {
    switch(actions.type) {

    }
}

export default authenticationReducer;